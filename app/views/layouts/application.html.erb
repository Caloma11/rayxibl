<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Flxibl</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600&display=swap" rel="stylesheet">
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'shared/flashes' %>
    <div class="yielded-content">
      <div class="contained">
        <%= yield %>
      </div>
    </div>
    <% if user_signed_in? %>
      <%= link_to "SIGN OUT BUTTON", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "SIGN IN BUTTON", new_user_session_path %>
    <% end %>
    <% if user_signed_in? && !(params[:controller] == "conversations" && params[:action] == "show") %>
      <%= render partial: "shared/navbar" %>
    <% end %>
  </body>
</html>
